#include "catch.hpp"
#include "Prompt.hpp"

// Comenzar/Parar: para indicar la actividad o no del móvil
// Reporte: para visualizar en pantalla la información de estado del móvil
// Izquierda/Derecha/Avanzar/Retroceder: para desplazar al móvil según una dirección desde
// el punto relativo en el que se encuentra. Estas órdenes van acompañadas de la cantidad de
// pasos a dar.
// Girar: para cambiar (rotar) el sentido frontal del móvil. Estas órdenes van acompañadas del
// sentido de giro que corresponda (Horario/Antihorario)

TEST_CASE( "Factorials are computed", "[factorial]" ) {
    REQUIRE( 1 == 1 );
    REQUIRE( 2 == 2 );
    REQUIRE( 6 == 6 );
}